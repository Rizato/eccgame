<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="32" height="32">
  <defs>
    <filter id="shadowIco" x="-20%" y="-20%" width="140%" height="140%">
      <feDropShadow dx="1" dy="1" stdDeviation="2" flood-color="rgba(0,0,0,0.3)"/>
    </filter>
  </defs>

  <!-- Main circle with dark mode purple-blue -->
  <circle cx="16" cy="16" r="14" fill="#0f172a" filter="url(#shadowIco)" stroke="#334155" stroke-width="1"/>

  <!-- ECC text -->
  <text x="16" y="19" font-family="system-ui, -apple-system, sans-serif" font-size="8" font-weight="bold" text-anchor="middle" fill="white">
    ECC
  </text>
</svg>
