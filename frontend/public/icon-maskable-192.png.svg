<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 192 192" width="192" height="192">
  <defs>
    <filter id="shadowMask192" x="-20%" y="-20%" width="140%" height="140%">
      <feDropShadow dx="3" dy="3" stdDeviation="4" flood-color="rgba(0,0,0,0.3)"/>
    </filter>
  </defs>

  <!-- Full background for maskable icon (safe area) -->
  <rect x="0" y="0" width="192" height="192" fill="#0f172a"/>

  <!-- Main circle - smaller to fit safe area -->
  <circle cx="96" cy="96" r="64" fill="#0f172a" filter="url(#shadowMask192)" stroke="#334155" stroke-width="3"/>

  <!-- ECC text -->
  <text x="96" y="105" font-family="system-ui, -apple-system, sans-serif" font-size="32" font-weight="bold" text-anchor="middle" fill="white">
    ECC
  </text>
</svg>
